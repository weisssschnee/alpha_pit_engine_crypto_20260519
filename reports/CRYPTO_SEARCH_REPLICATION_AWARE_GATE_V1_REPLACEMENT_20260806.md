# Crypto Replication-Aware Search Gate V1 Replacement — Invalid Run Closure

## Decision

`RUN_INVALID_NATIVE_STDERR_TERMINATED_BEFORE_WORKER_SUBMIT`

The one authorized replacement run produced no market-evaluation evidence. It
cannot compare typed random, current Evolution, and replication-aware Evolution.

## Frozen identity

- Producer SHA: `a0c60ec55c4e71da08f575dfcbf2ec76cecd7596`
- Receipt SHA256: `8783957DE8CFBA99B5CE80F1AAF492E2348045A562E1BBBB2352ACF1B7D81167`
- PC2 task: `job_20260806_125929_7681d5`
- Budget: `512 strict × 3 arms = 1,536 strict`
- Scope: 4h, binary/two-axis, development train only
- Unchanged: 115-field aligned carrier, Binance USD-M target, mapping, 5 bps
  cost, matched controls, catalog, compiler, AST, evaluator, and seeds
- Forbidden and not performed: validation, OOS, holdout, tuning, reseed,
  rescue rerun, automatic expansion, and promotion

## Terminal evidence

The exact producer wrote the frozen contract, embedded preflight, one
pre-submit proposal-batch receipt, and two worker-stage receipts. Windows
PowerShell then converted a NumPy `RuntimeWarning` written to native stderr into
a terminating `NativeCommandError`. The producer PID disappeared during the
first multiprocessing spawn and left one child holding inherited log handles.

The process was observed for five minutes with unchanged heartbeat, stdout,
CPU, and counters. Only the exact orphan worker was then terminated so the one
detached task could persist its terminal failure.

| Observation | Value |
|---|---:|
| Generation attempts | 8 |
| Submitted to workers | 0 |
| Returned from workers | 0 |
| Strict evaluated | 0 |
| Market evaluations | 0 |
| Checkpoints | 0 |
| Validation/OOS/holdout reads | 0 |
| Rescue rerun | false |

PC2 and local independent checkers both returned `FAIL` with the same 13
missing terminal artifacts. This is required behavior for the incomplete
runtime.

## Evidence identity

The 16-file evidence set is bound by
`runtime/crypto_search_replication_aware_gate_v1_20260806r1/invalid_run_artifact_manifest.json`.
Its sorted path/size/hash bundle identity is:

`C8B3ADD62EEBC1C01A9A7E0D200571485BED83FF582B57AD798CE2E5568720EC`

The replacement receipt is consumed as
`RUN_AUTHORIZATION_CONSUMED_ENGINE_VERIFICATION_FAILED` with
`run_authorized=false`.

## Research boundary

No reward, behavior-family, replication, productivity, Alpha, validation, OOS,
or promotion conclusion exists. The prior V1.1 development/validation and
family-consensus closures remain the latest checker-backed market evidence.

The wrapper failure is operationally understood, but this receipt forbids a
rescue rerun. Any further market run requires new explicit authorization and a
new receipt. This closure grants neither.
